# Cloudflare Pages configuration - no deploy command needed
# Cloudflare Pages handles deployment automatically from GitHub

# This file is intentionally empty for Pages projects
# Build is handled via Cloudflare Dashboard → Build settings
